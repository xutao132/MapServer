、首先接收客户端发送过来的消息json字符串ReceiveMessage();
、接收到该字符串以后，用该消息字符串进行各种处理
	2.0、查看消息类型（type）如果是200：表示登录消息，如果是210：是注册消息
	2.1、使用接收到的account账户和密码去与数据库查询是的账户（account）和密码（password）进行比较是否正确
	2.2、如果正确向客户端打印message：登录成功，反之登录失败
	“一个账号只能同时在一台设备登录”